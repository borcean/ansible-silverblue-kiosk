#!/bin/sh

if [ ! "$(pidof firefox)" ]
then
    /usr/bin/killall -u kiosk firefox
    /usr/bin/rm -rf ~/.cache/mozilla ~/.mozilla
    /usr/bin/firefox -kiosk -private-window {{ kiosk_url }}
fi

sleep 1.0
exec "$0" "$@"